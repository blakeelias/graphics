#ifndef SPLINE_DEF
#define SPLINE_DEF

void draw_hermite(struct matrix *points,
		  double p1_x, double p1_y,
		  double p2_x, double p2_y,
		  double p3_x, double p3_y,
		  double p4_x, double p4_y);

void draw_bezier(struct matrix *points,
		 double p1_x, double p1_y,
		 double p2_x, double p2_y,
		 double p3_x, double p3_y,
		 double p4_x, double p4_y);

void draw_spline(struct matrix *points,
		 double p1_x, double p1_y,
		 double p2_x, double p2_y,
		 double p3_x, double p3_y,
		 double p4_x, double p4_y,
		 struct matrix *M);

#endif
