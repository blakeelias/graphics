import java.awt.*;
import javax.swing.*;
import java.awt.event.*;
import java.awt.image.*;
import javax.imageio.*;

import java.io.*;
import java.util.*;

public class Gui extends JFrame
{
  public Gui()
  {
    Canvas c = new Canvas();
    add(c);
    setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    setSize((int)c.getPreferredSize().getWidth(), (int)c.getPreferredSize().getHeight());
    setLocationRelativeTo(null);
    setTitle("Transformations");
    setResizable(false);
    setVisible(true);
   // c.createInteractions();
  }

  public static void main(String[] args) 
  {   Gui g = new Gui();    }
}
